name: staging
on:
  push:
    branches:
      - tim-staging

env:
  GO111MODULE: on

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: ls
        run: ls
      
      - name: pwd
        run: pwd